# Project Instructions

- **Build Requirement:** ALWAYS run `npm run build` after making any code changes to ensure the build artifacts are updated. This is mandatory for this project.
- **llamacpp Configuration:** `llamacpp` uses the `apiKey` state field for its `Server URL`. Do not require an API key for `llamacpp`.
